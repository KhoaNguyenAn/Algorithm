#include <iostream>
#define INF 9999999
const int N = 1e5 + 7;
using namespace std;
int n,a[N],f1[N],f2[N],pre[N],suf[N],mmax = 0;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> m;
    for i
}

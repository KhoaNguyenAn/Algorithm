#include <iostream>

using namespace std;
int n,x,y,m;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> x >> y >> m;
    for (int i = 1; i <= n ;i++)
   //    cin >> a[i].x >> a[i].y >> a[i].z;
    return 0;

}

#include <iostream>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> k;
    cin >> n;
    cin >> m;
    for (int i = 1; i <= m ; i++)
        cin >> a[i].s >> a[i].d >> a[i].l >> a[i].c;
    for (int i = 1; i <= k ; i++)
      for (int j = 1; j <= n; j++)
        {
            for (int l = 1; l <= a[j].size(), l++)
            {

            }
        }
}
